# resinci-client Secrets

In this directory, you need to add the following files:

* `ca.pem`
* `cert.pem`
* `key.pem`

They should be the correct client certs to match the server certs on your
remote resinci-server instance.

